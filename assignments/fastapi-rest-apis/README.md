# 📘 Assignment: Building REST APIs with FastAPI

## 🎯 Objective

In this assignment, students will build a simple REST API using FastAPI. They will define endpoints, validate request data, and return clear JSON responses.

## 📝 Tasks

### 🛠️ Create Core API Endpoints

#### Description
Set up a FastAPI application and implement core endpoints for a basic resource (for example, books or tasks).

#### Requirements
Completed program should:

- Create a FastAPI app instance and run it locally
- Implement at least three endpoints: one `GET`, one `POST`, and one `DELETE`
- Return valid JSON responses with appropriate HTTP status codes


### 🛠️ Add Data Validation and Error Handling

#### Description
Improve the API by adding request validation and clear error responses.

#### Requirements
Completed program should:

- Define a Pydantic model for the `POST` request body
- Validate required fields and data types for incoming data
- Return a clear error response when a resource is not found

